diff --git a/flashinfer/cute_dsl/sparse/sm100_blk64/bindings.cpp b/flashinfer/cute_dsl/sparse/sm100_blk64/bindings.cpp
index 0be76fb..fdc7d13 100644
--- a/flashinfer/cute_dsl/sparse/sm100_blk64/bindings.cpp
+++ b/flashinfer/cute_dsl/sparse/sm100_blk64/bindings.cpp
@@ -5,11 +5,11 @@
 std::vector<torch::Tensor> bsa_fused_fwd_blk64(torch::Tensor q, torch::Tensor k, torch::Tensor v,
                                                torch::Tensor q2k_block_index, int block_sparse_num,
                                                torch::Tensor block_sizes, float softmax_scale,
-                                               torch::Tensor q2k_block_nums);
+                                               torch::Tensor q2k_block_nums, int num_heads_kv);
 
 PYBIND11_MODULE(TORCH_EXTENSION_NAME, m) {
   m.def("bsa_fused_fwd_blk64", &bsa_fused_fwd_blk64, "BSA fused attention forward blk=64 (C++ AOT)",
         py::arg("q"), py::arg("k"), py::arg("v"), py::arg("q2k_block_index"),
         py::arg("block_sparse_num"), py::arg("block_sizes"), py::arg("softmax_scale"),
-        py::arg("q2k_block_nums") = torch::Tensor());
+        py::arg("q2k_block_nums") = torch::Tensor(), py::arg("num_heads_kv") = 0);
 }
diff --git a/flashinfer/cute_dsl/sparse/sm100_blk64/flash_fwd_launch_template.cu b/flashinfer/cute_dsl/sparse/sm100_blk64/flash_fwd_launch_template.cu
index fd8a341..bdd7da6 100644
--- a/flashinfer/cute_dsl/sparse/sm100_blk64/flash_fwd_launch_template.cu
+++ b/flashinfer/cute_dsl/sparse/sm100_blk64/flash_fwd_launch_template.cu
@@ -17,13 +17,13 @@ template <bool HasVarBlockNums, bool HasBlockSizes>
 std::vector<torch::Tensor> bsa_fused_fwd_blk64_launch(
     torch::Tensor q, torch::Tensor k, torch::Tensor v, torch::Tensor q2k_block_index,
     int block_sparse_num, torch::Tensor block_sizes, float softmax_scale,
-    torch::Tensor q2k_block_nums);
+    torch::Tensor q2k_block_nums, int num_heads_kv);
 
 // Entry point: BOOL_SWITCH dispatches to the correct template instantiation
 inline std::vector<torch::Tensor> bsa_fused_fwd_blk64_impl(
     torch::Tensor q, torch::Tensor k, torch::Tensor v, torch::Tensor q2k_block_index,
     int block_sparse_num, torch::Tensor block_sizes, float softmax_scale,
-    torch::Tensor q2k_block_nums) {
+    torch::Tensor q2k_block_nums, int num_heads_kv) {
   TORCH_CHECK(q.is_cuda() && k.is_cuda() && v.is_cuda(), "q/k/v must be CUDA");
   TORCH_CHECK(q.dim() == 4 && k.dim() == 4 && v.dim() == 4, "q/k/v must be 4D");
   TORCH_CHECK(q.size(3) == 128, "requires D=128");
@@ -33,7 +33,7 @@ inline std::vector<torch::Tensor> bsa_fused_fwd_blk64_impl(
   return BOOL_SWITCH(has_variable_block_nums, HasVarBlockNums, [&] {
     return BOOL_SWITCH(has_block_sizes, HasBlockSizes, [&] {
       return bsa_fused_fwd_blk64_launch<HasVarBlockNums, HasBlockSizes>(
-          q, k, v, q2k_block_index, block_sparse_num, block_sizes, softmax_scale, q2k_block_nums);
+          q, k, v, q2k_block_index, block_sparse_num, block_sizes, softmax_scale, q2k_block_nums, num_heads_kv);
     });
   });
 }
@@ -44,7 +44,7 @@ inline std::vector<torch::Tensor> bsa_fused_fwd_blk64_impl(
 std::vector<torch::Tensor> bsa_fused_fwd_blk64(torch::Tensor q, torch::Tensor k, torch::Tensor v,
                                                torch::Tensor q2k_block_index, int block_sparse_num,
                                                torch::Tensor block_sizes, float softmax_scale,
-                                               torch::Tensor q2k_block_nums) {
+                                               torch::Tensor q2k_block_nums, int num_heads_kv) {
   return flash::bsa_fused_fwd_blk64_impl(q, k, v, q2k_block_index, block_sparse_num, block_sizes,
-                                         softmax_scale, q2k_block_nums);
+                                         softmax_scale, q2k_block_nums, num_heads_kv);
 }
diff --git a/flashinfer/cute_dsl/sparse/sm100_blk64/flash_fwd_launch_template.h b/flashinfer/cute_dsl/sparse/sm100_blk64/flash_fwd_launch_template.h
index 2a194a8..90e937c 100644
--- a/flashinfer/cute_dsl/sparse/sm100_blk64/flash_fwd_launch_template.h
+++ b/flashinfer/cute_dsl/sparse/sm100_blk64/flash_fwd_launch_template.h
@@ -26,7 +26,7 @@ template <bool HasVarBlockNums, bool HasBlockSizes>
 std::vector<torch::Tensor> bsa_fused_fwd_blk64_launch(
     torch::Tensor q, torch::Tensor k, torch::Tensor v, torch::Tensor q2k_block_index,
     int block_sparse_num, torch::Tensor block_sizes, float softmax_scale,
-    torch::Tensor q2k_block_nums) {
+    torch::Tensor q2k_block_nums, int num_heads_kv = 0) {
   using namespace cute;
 
   using Kernel = FusedAttnKernel<HasVarBlockNums, HasBlockSizes>;
@@ -80,13 +80,26 @@ std::vector<torch::Tensor> bsa_fused_fwd_blk64_launch(
   q_padded.narrow(2, 0, seq_q).copy_(q_bhsd);
   auto q_tiled = q_padded.view({batch * heads * num_row_tiles, kRows, kQkK}).contiguous();
 
-  // ======== Prepare K ========
+  // ======== Prepare K/V ========
+  // Paged mode: k and v are already the page pool the kernel indexes, so none of
+  // the permute/pad/contiguous work below runs. It is the dominant host cost at
+  // long sequence (hundreds of MB copied per call at 32K x 32 heads).
+  bool const kv_paged = num_heads_kv > 0;
+  int const num_pages = kv_paged ? static_cast<int>(k.size(0)) / (num_heads_kv * 2) : 0;
+  torch::Tensor k_blocks, v_blocks;
+  if (kv_paged) {
+    TORCH_CHECK(k.dim() == 3 && v.dim() == 3,
+                "paged blk64 expects k/v as [num_pages*H_kv*2, 64, 64]");
+    TORCH_CHECK(k.size(0) % (num_heads_kv * 2) == 0, "k pool not a multiple of H_kv*2");
+    k_blocks = k.contiguous();
+    v_blocks = v.contiguous();
+  } else {
   auto k_bhsd = k.permute({0, 2, 1, 3}).contiguous();
   int const total_k_padded = ((seq_k + kSparseBlockSize - 1) / kSparseBlockSize) * kSparseBlockSize;
   int const total_sparse_blocks = total_k_padded / kSparseBlockSize;
   auto k_padded = torch::zeros({batch, heads, total_k_padded, kQkK}, k.options());
   k_padded.narrow(2, 0, seq_k).copy_(k_bhsd);
-  auto k_blocks =
+  k_blocks =
       k_padded.view({batch, heads, total_sparse_blocks, kSparseBlockSize, 2, kDimHalf})
           .permute({0, 1, 2, 4, 3, 5})
           .reshape({batch * heads * total_sparse_blocks * 2, kSparseBlockSize, kDimHalf})
@@ -96,11 +109,12 @@ std::vector<torch::Tensor> bsa_fused_fwd_blk64_launch(
   auto v_bhds = v.permute({0, 2, 3, 1}).contiguous();
   auto v_padded = torch::zeros({batch, heads, kOutputCols, total_k_padded}, v.options());
   v_padded.narrow(3, 0, seq_k).copy_(v_bhds);
-  auto v_blocks =
+  v_blocks =
       v_padded.view({batch, heads, 2, kDimHalf, total_sparse_blocks, kSparseBlockSize})
           .permute({0, 1, 4, 2, 3, 5})
           .reshape({batch * heads * total_sparse_blocks * 2, kDimHalf, kSparseBlockSize})
           .contiguous();
+  }
 
   auto out_flat = torch::zeros({batch * heads * num_row_tiles, kRows, kOutputCols}, q.options());
   auto lse_flat =
@@ -133,6 +147,8 @@ std::vector<torch::Tensor> bsa_fused_fwd_blk64_launch(
           block_sizes.defined() && block_sizes.numel() > 0 ? block_sizes.data_ptr<int>() : nullptr,
           ptr_q2k_block_nums,
           raw_block_sparse_num,
+          num_heads_kv,
+          num_pages,
       },
       // epilogue
       {reinterpret_cast<bf16*>(out_flat.data_ptr<at::BFloat16>()), lse_flat.data_ptr<float>()},
diff --git a/flashinfer/cute_dsl/sparse/sm100_blk64/mainloop_fwd_sm100.hpp b/flashinfer/cute_dsl/sparse/sm100_blk64/mainloop_fwd_sm100.hpp
index 39cee30..d2238ae 100644
--- a/flashinfer/cute_dsl/sparse/sm100_blk64/mainloop_fwd_sm100.hpp
+++ b/flashinfer/cute_dsl/sparse/sm100_blk64/mainloop_fwd_sm100.hpp
@@ -155,6 +155,8 @@ struct CollectiveMainloopFwd {
     int const* ptr_q2k_block_nums =
         nullptr;  // variable: [batch * heads, num_q_blocks], per-tile sparse block count
     int raw_block_sparse_num = 0;  // original unpadded block_sparse_num (for phantom clamping)
+    int num_heads_kv = 0;          // 0 => dense/legacy: KV is indexed per query head
+    int num_pages = 0;             // physical pages in the KV cache (paged mode)
   };
 
   // ---- Params (device-side) ----
@@ -169,6 +171,8 @@ struct CollectiveMainloopFwd {
     int const* ptr_block_indices = nullptr;
     int block_indices_stride = 0;
     int kv_tma_per_head = 0;
+    int num_heads_kv = 0;    // 0 => legacy dense path (num_heads_kv == num_heads)
+    int qhead_per_kv = 1;    // num_heads / num_heads_kv
     int const* ptr_block_sizes = nullptr;
     int num_heads = 1;
     int num_row_tiles = 1;
@@ -193,15 +197,16 @@ struct CollectiveMainloopFwd {
         SmemLayoutQ{});
 
     int const total_sparse_blocks = seq_kv / kSparseBlockSize;
-    auto shape_k =
-        make_shape(kSparseBlockSize, kDimHalf, batch * heads * total_sparse_blocks * kDimHalves);
+    int const kv_planes = (args.num_heads_kv > 0)
+                              ? args.num_pages * args.num_heads_kv * kDimHalves
+                              : batch * heads * total_sparse_blocks * kDimHalves;
+    auto shape_k = make_shape(kSparseBlockSize, kDimHalf, kv_planes);
     auto stride_k = make_stride(Int<kDimHalf>{}, _1{}, kSparseBlockSize * kDimHalf);
     auto tma_k = make_tma_copy(
         SM90_TMA_LOAD{}, make_tensor(make_gmem_ptr(args.ptr_K), make_layout(shape_k, stride_k)),
         SmemLayoutSubTile{});
 
-    auto shape_v =
-        make_shape(kDimHalf, kSparseBlockSize, batch * heads * total_sparse_blocks * kDimHalves);
+    auto shape_v = make_shape(kDimHalf, kSparseBlockSize, kv_planes);
     auto stride_v = make_stride(Int<kSparseBlockSize>{}, _1{}, kDimHalf * kSparseBlockSize);
     auto tma_v = make_tma_copy(
         SM90_TMA_LOAD{}, make_tensor(make_gmem_ptr(args.ptr_V), make_layout(shape_v, stride_v)),
@@ -219,6 +224,8 @@ struct CollectiveMainloopFwd {
             args.ptr_block_indices,
             args.block_indices_stride,
             kv_tma_per_head,
+            args.num_heads_kv,
+            (args.num_heads_kv > 0 ? heads / args.num_heads_kv : 1),
             args.ptr_block_sizes,
             heads,
             num_row_tiles,
@@ -294,7 +301,13 @@ struct CollectiveMainloopFwd {
 
       // Batch-aware TMA indices: batch folds into outermost dimension
       int q_tile_idx = (batch * params.num_heads + head) * num_row_tiles + row_tile;
-      int kv_tma_base = (batch * params.num_heads + head) * params.kv_tma_per_head;
+      // Paged + GQA: K/V live in a global page pool indexed by physical page and
+      // KV head, so there is no batch term and no per-query-head replication.
+      // Legacy (num_heads_kv == 0) keeps the dense per-(batch, head) layout.
+      bool const kv_paged = params.num_heads_kv > 0;
+      int const kv_head = kv_paged ? (head / params.qhead_per_kv) : head;
+      int kv_tma_base = kv_paged ? 0
+                                 : (batch * params.num_heads + head) * params.kv_tma_per_head;
 
       // Block indices for this (batch, head, row_tile)
       int const* tile_block_indices = nullptr;
@@ -349,7 +362,9 @@ struct CollectiveMainloopFwd {
             int smem_offset = stage_base + slot * kKSubStride + h * kKHalfStride;
             auto sK_sub =
                 make_tensor(make_smem_ptr(ml.smem_kv.begin() + smem_offset), SmemLayoutSubTile{});
-            int tma_idx = kv_tma_base + sparse_idx * kDimHalves + h;
+            int tma_idx = kv_paged
+                              ? (sparse_idx * params.num_heads_kv + kv_head) * kDimHalves + h
+                              : kv_tma_base + sparse_idx * kDimHalves + h;
             cute::copy(params.tma_load_K.with(*tma_bar),
                        thr_tma_k.partition_S(gK_full(_, _, tma_idx)),
                        thr_tma_k.partition_D(sK_sub));
@@ -372,7 +387,9 @@ struct CollectiveMainloopFwd {
             int smem_offset = stage_base + slot * kVSubStride + h * kVHalfStride;
             auto sV_sub =
                 make_tensor(make_smem_ptr(ml.smem_kv.begin() + smem_offset), SmemLayoutSubTile{});
-            int tma_idx = kv_tma_base + sparse_idx * kDimHalves + h;
+            int tma_idx = kv_paged
+                              ? (sparse_idx * params.num_heads_kv + kv_head) * kDimHalves + h
+                              : kv_tma_base + sparse_idx * kDimHalves + h;
             cute::copy(params.tma_load_V.with(*tma_bar),
                        thr_tma_v.partition_S(gV_full(_, _, tma_idx)),
                        thr_tma_v.partition_D(sV_sub));
